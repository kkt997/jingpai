# 后端商品接口规范

用于移动端和管理端调用以获取指定商品的详细信息。

## 1. 获取商品详情

获取指定商品 ID 的详细图文资料，包括图片数组、详细文字描述等。

- **请求方式**：`GET`
- **接口路径**：`/api/products/:id`
- **鉴权要求**：需携带 JWT Token（在请求头中传入 `Authorization: Bearer <token>`）

### 1.1 请求参数（路径参数 Path Parameter）

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | 是 | 商品主键 ID |

### 1.2 成功响应 (HTTP 200 OK)

返回统一的响应格式，`code` 为 0 时代表成功，数据包裹在 `data` 属性内。

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "id": 1,
    "merchantId": 2,
    "title": "皇家帝王绿翡翠无事牌吊坠",
    "description": "精选老坑冰种帝王绿翡翠，水头十足，质地细腻纯净。无事牌造型简约大气，寓意平安无事、万事顺遂。附带国家权威珠宝检测证书，支持全国复检。",
    "images": [
      "https://example.com/images/jade-1.jpg",
      "https://example.com/images/jade-2.jpg",
      "https://example.com/images/jade-3.jpg"
    ],
    "category": "珠宝玉石",
    "createdAt": "2026-06-04T12:00:00Z"
  }
}
```

### 1.3 错误响应 (HTTP 200 或相应 HTTP 状态码)

| 状态码 | 业务错误码 code | 说明 |
| :--- | :--- | :--- |
| 404 | 4004 | 商品未找到 |
| 401 | 4011 | 未登录或 Token 失效 |
| 500 | 5000 | 服务器内部错误 |

```json
{
  "code": 4004,
  "msg": "商品不存在",
  "data": null
}
```

---

## 2. WebSocket 聊天弹幕协议

用于直播间内的聊天弹幕发送与全员广播交互。

### 2.1 客户端发送弹幕 (C2S)

当用户在 H5 端输入文字并点击发送时，通过 WebSocket 连接发送此消息。

- **消息类型 `type`**：`chat`
- **消息载荷 `payload`**：
  - `message`: 弹幕文本内容 (STRING, 最大限制 100 字符)

#### 示例数据

```json
{
  "type": "chat",
  "seq": 10,
  "payload": {
    "message": "老坑翡翠就是好看，起拍价多少？"
  }
}
```

### 2.2 服务端广播弹幕 (S2C)

当服务端接收到某位用户的弹幕消息后，自动加上该用户的虚拟昵称并广播给直播间（Room）内的所有人。

- **消息类型 `type`**：`chat_message`
- **消息主体 `data`**：
  - `nickname`: 发送人的虚拟昵称 (STRING，若为系统发送的出价动态，则昵称固定为 `系统`)
  - `message`: 弹幕文本内容 (STRING)
  - `isMe`: 是否为当前接收者自己发送的弹幕 (BOOLEAN, 服务端广播时可根据 WS 连接归属动态计算，或者由客户端对比自己的别名自行处理)
  - `createdAt`: 发生时间戳 (STRING)

#### 示例数据

```json
{
  "type": "chat_message",
  "code": 0,
  "msg": "success",
  "data": {
    "nickname": "神秘买家2",
    "message": "老坑翡翠就是好看，起拍价多少？",
    "isMe": false,
    "createdAt": "2026-06-04T12:05:00Z"
  },
  "ts": 1716624300000
}
```

---

## 3. 获取直播间拍品柜清单

获取指定直播间 ID 的完整拍品柜展示清单，包括拍过已成交的历史拍品、当前正在进行的拍品、以及待竞拍的后面拍品。

- **请求方式**：`GET`
- **接口路径**：`/api/rooms/:roomId/showcase`
- **鉴权要求**：需携带 JWT Token

### 3.1 请求参数（路径参数 Path Parameter）

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| `roomId` | BIGINT | 是 | 直播间主键 ID |

### 3.2 成功响应 (HTTP 200 OK)

```json
{
  "code": 0,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "productId": 10,
      "product": {
        "id": 10,
        "title": "老坑冰种帝王绿无事牌",
        "description": "老坑冰种帝王绿翡翠，质地纯净细腻，无事牌造型简约大气。",
        "images": ["https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?w=500&auto=format&fit=crop&q=60"],
        "category": "珠宝玉石"
      },
      "status": "COMPLETED",
      "startingPrice": 5000,
      "currentPrice": 9800,
      "finalPrice": 9800,
      "winnerNickname": "神秘买家2",
      "sequence": 1
    },
    {
      "id": 2,
      "productId": 11,
      "product": {
        "id": 11,
        "title": "和田玉白玉牌观音吊坠",
        "description": "新疆和田玉白玉观音挂件，雕工精细，玉质温润。",
        "images": ["https://images.unsplash.com/photo-1617038260897-41a1f14a8ca0?w=500&auto=format&fit=crop&q=60"],
        "category": "珠宝玉石"
      },
      "status": "ACTIVE",
      "startingPrice": 3000,
      "currentPrice": 4200,
      "sequence": 2
    },
    {
      "id": 3,
      "productId": 12,
      "product": {
        "id": 12,
        "title": "红蓝宝石复古18K金戒指",
        "description": "古典奢华红蓝宝石配饰，精细戒托，送礼佳品。",
        "images": ["https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=500&auto=format&fit=crop&q=60"],
        "category": "奢侈配饰"
      },
      "status": "PENDING",
      "startingPrice": 8000,
      "sequence": 3
    }
  ]
}
```

---

## 4. 获取直播间详情

获取指定直播间 ID 的详细资料及当前关联的竞拍数据，用于页面进入时的全量渲染。

- **请求方式**：`GET`
- **接口路径**：`/api/rooms/:id`
- **鉴权要求**：需携带 JWT Token

### 4.1 请求参数（路径参数 Path Parameter）

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | 是 | 直播间主键 ID |

### 4.2 成功响应 (HTTP 200 OK)

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "id": 1,
    "merchantId": 2,
    "roomTitle": "精品珠宝翡翠专场直播间",
    "streamUrl": "https://example.com/live/room-1.m3u8",
    "status": "LIVE",
    "onlineCount": 18,
    "auction": {
      "id": 2,
      "productId": 11,
      "roomId": 1,
      "merchantId": 2,
      "mode": "OPEN",
      "startingPrice": 3000,
      "incrementAmount": 100,
      "ceilingPrice": 50000,
      "durationSeconds": 300,
      "autoExtendSeconds": 30,
      "status": "ACTIVE",
      "currentPrice": 4200,
      "winnerId": null,
      "bidCount": 6,
      "extendCount": 0,
      "depositRequired": true,
      "depositAmount": 500,
      "hasDeposit": true,
      "product": {
        "id": 11,
        "title": "和田玉白玉牌观音吊坠",
        "description": "新疆和田玉白玉观音挂件，雕工精细，玉质温润。",
        "images": ["https://images.unsplash.com/photo-1617038260897-41a1f14a8ca0?w=500&auto=format&fit=crop&q=60"],
        "category": "珠宝玉石"
      },
      "ranking": [
        {
          "rank": 1,
          "alias": "神秘买家1",
          "amount": 4200,
          "isMe": false
        },
        {
          "rank": 2,
          "alias": "巅峰竞拍者2",
          "amount": 4100,
          "isMe": true
        }
      ]
    }
  }
}
```

---

## 5. 缴纳竞拍保证金

为当前活跃竞拍场次缴纳指定金额的保证金。

- **请求方式**：`POST`
- **接口路径**：`/api/deposits`
- **鉴权要求**：需携带 JWT Token
- **请求载荷 `body` (JSON)**：
  - `auctionId`: 竞拍场次 ID (BIGINT, 必填)

### 5.1 请求体示例

```json
{
  "auctionId": 2
}
```

### 5.2 成功响应 (HTTP 200 OK)

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "id": 88,
    "userId": 5,
    "auctionId": 2,
    "amount": 500.00,
    "status": "PAID",
    "createdAt": "2026-06-04T15:30:00Z"
  }
}
```

---

## 6. 模拟支付订单

模拟对中标成交的订单进行在线支付。

- **请求方式**：`POST`
- **接口路径**：`/api/orders/:id/pay`
- **鉴权要求**：需携带 JWT Token

### 6.1 请求参数（路径参数 Path Parameter）

| 参数名 | 类型 | 必选 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | 是 | 订单主键 ID |

### 6.2 成功响应 (HTTP 200 OK)

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "id": 99,
    "orderNo": "ORD20260604123456",
    "auctionId": 2,
    "buyerId": 5,
    "finalPrice": 9800.00,
    "status": "PAID",
    "paymentTime": "2026-06-04T15:45:00Z"
  }
}
```

```


