# 竞拍大师项目修复方案

> 目标：优先修复会导致价格泄露、出价丢失、错账、错单、竞拍无法结束的问题；再补齐状态一致性、前端实时同步和工程质量。

## 一、修复优先级

| 优先级 | 范围 | 目标 |
|---|---|---|
| P0 | 数据安全、核心链路 | 先解决盲拍价格泄露、出价记录丢失、服务重启后竞拍 timer 丢失 |
| P1 | 业务一致性 | 修复竞拍创建/开始/结束、保证金、商家自买、直播间生命周期问题 |
| P2 | 前端体验和工程质量 | 修复前端状态同步、错误码、测试覆盖和配置一致性 |

## 二、P0 必修项

### 1. 修复盲拍价格泄露 ✅ 已完成

**状态**

已完成。后端已统一 WS 广播、WS `room_state`、HTTP 竞拍详情/列表/拍品柜的盲拍脱敏逻辑；普通用户看不到 `currentPrice` 和他人 `amount`，商家端仍可见完整金额。

**问题**

- `backend/internal/service/broadcast.go` 的盲拍广播仍然向所有用户发送 `amount` 和 `currentPrice`。
- `backend/internal/handler/ws_handler.go` 的 `room_state` 在盲拍模式下仍返回 `currentPrice`。

**影响**

普通用户可以通过 WebSocket 消息或房间全量状态看到盲拍最高价，破坏盲拍规则。

**修复方案**

- 在盲拍模式下，普通用户广播消息只保留：
  - `bidCount`
  - `ranking`
  - 自己的 `amount`
  - 自己的 `rank`
- 普通用户不返回 `currentPrice` 和他人 `amount`。
- 商家端可以看到完整金额。
- HTTP 获取竞拍详情和 WS `room_state` 使用同一套脱敏逻辑，避免接口绕过。

**验收标准**

- 普通用户在盲拍中看不到 `currentPrice`。
- 普通用户只能看到自己的出价金额。
- 商家端仍能看到完整排行榜金额。

### 2. 修复出价持久化丢失 ✅ 已完成

**状态**

已完成。出价 Redis Lua 成功路径已同步写入 `auction:{auctionId}:bid_stream`，并登记到 `auction:bid_streams`；后台 worker 使用 consumer group 消费、MySQL 幂等写入、成功后 ACK，失败保留 pending 并自动重试/认领，避免 accepted bid 因 channel 满、DB 临时失败或服务重启丢失。

**问题**

- `backend/internal/service/bid.go` 中 `persistCh` 满了会直接丢弃已接受的出价。
- MySQL 批量写失败只打日志，没有重试或补偿。

**影响**

Redis 已接受出价并广播成功，但 MySQL 中可能没有对应 bid 记录，违背“零数据差错”目标。

**修复方案**

推荐将异步 channel 改为可靠队列：

1. Redis Lua 出价成功后，把 bid 追加到 Redis Stream。
2. 后台 worker 从 Stream 消费并写入 MySQL。
3. MySQL 写入成功后 ACK。
4. 服务重启后继续消费未 ACK 记录。
5. 对写入失败的记录做重试和错误日志告警。

可选 Stream key：

```text
auction:{auctionId}:bid_stream
```

建议记录字段：

```text
auction_id, user_id, amount_cents, bid_time_ms, seq
```

**验收标准**

- worker 停止后继续出价，恢复 worker 后 bid 能补写入 MySQL。
- MySQL 暂时失败时，accepted bid 不丢失。
- 压测下不会出现 Redis 排行榜有记录但 MySQL bids 缺记录。

### 3. 修复服务重启后活动竞拍 timer 丢失 ✅ 已完成

**状态**

已完成。`AuctionTimer.Start()` 会恢复 DB 中 `ACTIVE/EXTENDED` 竞拍；Redis 状态缺失时可从 DB 与 bids 重建实时状态；已过期竞拍会立即触发 timeout；并增加 15 秒兜底扫描防止 timer 漏注册。

**问题**

`AuctionTimer.Start()` 只启动倒计时同步循环，已存在的 ACTIVE/EXTENDED 竞拍不会在服务重启后恢复 timer。

**影响**

服务重启后，活动竞拍可能永远不会 timeout，也不会自动成交、流拍、生成订单或退款。

**修复方案**

启动时恢复活动竞拍：

1. 查询 DB 中状态为 `ACTIVE` 或 `EXTENDED` 的竞拍。
2. 从 Redis 读取 `auction:{id}:state.end_time`。
3. 若 Redis 不存在，按 DB 状态做异常修复或标记为待人工处理。
4. 若 `end_time <= now`，立即触发 timeout。
5. 若 `end_time > now`，重新注册 timer。
6. 增加定时兜底扫描，防止单个 timer 丢失。

**验收标准**

- 服务重启后，未结束竞拍能继续倒计时。
- 已过期竞拍会自动结算或流拍。
- 重启不会生成重复订单。

## 三、P1 业务一致性修复

### 4. 开始竞拍改为事务式状态切换 ✅ 已完成

**状态**

已完成。开始竞拍已改为行锁事务校验，检查 `PENDING` 状态、直播间 `LIVE` 状态、同房间无其他活动竞拍；Redis 初始化成功后才提交 DB 状态，失败会清理 Redis 并保持原状态。

**问题**

当前开始竞拍流程先更新 DB 为 ACTIVE，再初始化 Redis。如果 Redis 初始化失败，DB 和 Redis 状态会不一致。

**修复方案**

- 使用 DB transaction 和行锁。
- 校验竞拍仍为 `PENDING`。
- 校验直播间为 `LIVE`。
- 校验同房间无其他 `ACTIVE/EXTENDED` 竞拍。
- 初始化 Redis 成功后再提交 DB 状态。
- 任一步失败都不得留下 ACTIVE 的半成品状态。

**验收标准**

- Redis 初始化失败时，竞拍仍保持 PENDING。
- 并发点击开始竞拍时，只有一次成功。

### 5. 创建竞拍增加归属和唯一性校验 ✅ 已完成

**状态**

已完成。创建竞拍已校验商品归属、直播间归属、商品 `LISTED`、同商品无 `PENDING/ACTIVE/EXTENDED` 竞拍、同直播间无活动竞拍，且禁止在已结束直播间创建竞拍。

**问题**

创建竞拍只校验商品归属，没有校验直播间归属，也没有限制商品/房间重复竞拍。

**修复方案**

创建竞拍时增加校验：

- `product.merchant_id == currentMerchantId`
- `room.merchant_id == currentMerchantId`
- 商品状态必须为 `LISTED`
- 同一商品不能存在 `PENDING/ACTIVE/EXTENDED` 竞拍
- 同一直播间同时只能有一个 `ACTIVE/EXTENDED` 竞拍

**验收标准**

- 商家不能把自己的商品放进别人的直播间竞拍。
- 同一商品不能重复创建进行中竞拍。

### 6. 结束直播时处理活动竞拍 ✅ 已完成

**状态**

已完成。结束直播会联动处理房间内 `PENDING/ACTIVE/EXTENDED` 竞拍：待开始取消，活动竞拍有出价则结算、无出价则流拍；同时停止 timer、广播结束、处理保证金并清理 Redis 热状态。WS 加房与出价已校验直播间必须为 `LIVE`。

**问题**

结束直播只修改 room 状态并退款，没有结束房间内活动竞拍，也没有停止 timer。

**修复方案**

结束直播时：

- 找出房间内 `PENDING/ACTIVE/EXTENDED` 竞拍。
- `PENDING` 竞拍直接取消。
- `ACTIVE/EXTENDED` 按业务规则取消或强制结算。
- 停止 timer。
- 清理 Redis 状态。
- 广播 `auction_end`。
- 再处理保证金退款。

同时，WS 加房和出价时校验 room 状态，非 `LIVE` 房间不可继续出价。

**验收标准**

- 直播间 ENDED 后不能继续出价。
- 直播结束后不会残留活动竞拍 timer。

### 7. 禁止商家给自己出价 ✅ 已完成

**状态**

已完成。WS 出价路径已在保证金校验前拦截 `client.UserID == auction.MerchantID`，无保证金场次也无法绕过。

**问题**

保证金缴纳路径禁止商家参与自己的竞拍，但 WS 出价路径没有校验。无保证金场次可绕过。

**修复方案**

在 `ws_handler.go` 的出价逻辑中增加：

```go
if client.UserID == auction.MerchantID {
    // reject bid
}
```

**验收标准**

- 商家账号无法给自己创建的竞拍出价。
- 有无保证金场次都无法绕过。

### 8. 统一封顶成交和超时成交的保证金逻辑 ✅ 已完成

**状态**

已完成。超时成交、封顶成交和直播结束强制成交统一走 `handleAuctionCompleted`，按同一顺序创建订单、扣赢家保证金、退还非赢家保证金；订单按 `auction_id` 唯一幂等创建，避免重复订单。

**问题**

超时成交会扣赢家保证金再退其他人；封顶成交直接退全场保证金，两条成交路径账务不一致。

**修复方案**

把成交后保证金处理抽成统一方法：

```text
handleAuctionCompleted(auctionID, winnerID)
```

内部统一执行：

1. 生成订单。
2. 处理赢家保证金。
3. 退还非赢家保证金。
4. 写日志和指标。

**验收标准**

- 超时成交和封顶成交的保证金状态一致。
- 赢家和非赢家保证金状态符合业务规则。

## 四、P2 前端和工程质量修复

### 9. 修复前端 auction_start 后没有 auction 对象 ✅ 已完成

**状态**

已完成。后端 `auction_start` 广播已包含脱敏后的完整 `auction` 与 `product` 信息；前端收到 `auction_start` 后会写入完整 auction/product，并兼容旧字段格式。

**问题**

用户先进入直播间、主播后开始竞拍时，前端只收到状态和倒计时，没有完整 auction 对象。页面依赖 `auction.auction?.id` 判断是否有竞拍，可能无法显示竞拍卡片或出价。

**修复方案**

两种可选方案：

- 后端 `auction_start` 广播完整 auction 和 product 信息。
- 前端收到 `auction_start` 后主动重新拉取 `room_state` 或竞拍详情。

建议以后端补完整数据为主，前端拉取作为兜底。

**验收标准**

- 用户先进直播间，主播后开拍，用户端能立即看到竞拍卡片和出价按钮。

### 10. 修复 myRank 被清空 ✅ 已完成

**状态**

已完成。前端 `onRankingUpdate` 在 `myRank` 缺失时保留旧值，并优先从 `ranking[].isMe` 推导排名，避免领先/被超越/中奖状态被误清空。

**问题**

前端收到 `new_bid` 后，如果消息没有 `myRank`，会把 `myRank` 置为 null，影响领先状态、被超越提醒、中奖提示。

**修复方案**

- 前端 `onRankingUpdate` 不应在 `myRank` 缺失时清空原值。
- 更稳方案：后端对每个用户定制 `isMe`，前端从 ranking 中推导 `myRank`。

**验收标准**

- 用户领先后按钮能保持“已是最高价”状态。
- 被超越提醒能正常触发。
- 成交后中奖/未中奖提示准确。

### 11. WS send 失败不要静默成功 ✅ 已完成

**状态**

已完成。`WsClient.send()` 已改为返回 `{ ok, seq }`；出价 store 在未连接、连接中或重连中发送失败时会立即解除 pending，并给出明确提示。

**问题**

前端 `WsClient.send()` 在 socket 未连接时仍返回 seq，调用方会误以为消息已发送，出价按钮可能一直 pending。

**修复方案**

将 `send()` 改为返回发送结果：

```ts
type SendResult = { ok: boolean; seq: number };
```

当 socket 未打开时：

- 返回 `ok: false`
- 出价状态恢复为非 pending
- 提示用户正在重连或连接已断开

**验收标准**

- 断网或重连中点击出价不会卡死。
- 用户能看到明确提示。

### 12. 商品编辑和删除增加竞拍保护 ✅ 已完成

**状态**

已完成。商品编辑和删除前会检查该商品是否存在 `PENDING/ACTIVE/EXTENDED` 竞拍，存在则拒绝修改或删除，和下架保护保持一致。

**问题**

商品下架有活动竞拍检查，但编辑和删除没有同等保护。

**修复方案**

在商品更新、删除前检查：

```text
product_id = ? AND status IN ('PENDING', 'ACTIVE', 'EXTENDED')
```

若存在，禁止修改关键字段或删除。

**验收标准**

- 进行中竞拍商品不能被删除。
- 商品标题、图片、描述不会在竞拍中被随意改动。

### 13. 统一错误码 ✅ 已完成

**状态**

已完成。后端错误码已按 `1000-1999` 通用、`2000-2999` 鉴权权限、`4001-4099` 竞拍业务、`5000-5999` 系统错误分段；HTTP response helper、WS、限流中间件和前端 shared 常量已统一。

**问题**

HTTP 通用错误码和竞拍业务错误码存在冲突。例如 Forbidden 返回 4003，而 4003 同时代表出价太频繁。

**修复方案**

建议错误码分段：

| 范围 | 用途 |
|---|---|
| 0 | 成功 |
| 1000-1999 | 通用参数和资源错误 |
| 2000-2999 | 认证和权限错误 |
| 4001-4099 | 竞拍业务错误 |
| 5000-5999 | 系统错误 |

**验收标准**

- 前端可稳定按错误码处理业务分支。
- 文档、后端常量、前端类型保持一致。

## 五、建议测试清单

### 后端单元测试

- Redis Lua 出价测试：
  - 出价太低
  - 正常出价
  - 最高价用户重复出价
  - 触发延时
  - 达到封顶价
- 盲拍脱敏测试：
  - 普通用户不可见他人金额
  - 普通用户不可见当前最高价
  - 商家可见完整金额
- 状态机测试：
  - PENDING -> ACTIVE
  - ACTIVE -> EXTENDED
  - ACTIVE -> COMPLETED
  - ACTIVE -> FAILED
  - ACTIVE/EXTENDED -> CANCELLED
- 保证金测试：
  - 成交
  - 流拍
  - 取消
  - 封顶成交

### 集成测试

- 开始竞拍 Redis 初始化失败时 DB 回滚。
- 服务重启后 ACTIVE 竞拍自动恢复 timer。
- MySQL 临时失败后 bid 不丢失并可补写。
- 直播结束后不能继续出价。

### 前端测试

- 用户先进入直播间，主播后开拍。
- 断线重连后自动 `join_room` 并恢复全量状态。
- 盲拍页面不显示当前最高价。
- 断网时点击出价不会卡 pending。

## 六、建议执行顺序

1. 盲拍脱敏。
2. 出价可靠持久化。
3. timer 重启恢复。
4. 开始竞拍事务化。
5. 创建竞拍归属和唯一性校验。
6. 直播间结束联动竞拍结束。
7. 商家自买拦截。
8. 统一成交保证金逻辑。
9. 前端 `auction_start` 状态修复。
10. 前端 `myRank` 修复。
11. WS send 失败处理。
12. 商品编辑/删除保护。
13. 错误码统一和测试补齐。

## 七、阶段性验收目标

### 第一阶段

- 盲拍无价格泄露。
- accepted bid 不丢失。
- 服务重启后竞拍能自动结束。

### 第二阶段

- 竞拍创建、开始、结束全链路状态一致。
- 直播间结束后不会残留活动竞拍。
- 保证金账务路径一致。

### 第三阶段

- 前端实时状态稳定。
- 错误码清晰。
- 核心链路有测试覆盖。

## 八、二次检查与追加优化 ✅ 已完成

**检查结果**

完成 13 项修复后，已再次检查后端关键链路、错误码残留、订单幂等、前端构建和测试状态。

**发现的问题与修复方案**

1. `backend/internal/ws/hub.go` 仍残留硬编码旧错误码 `4010`。
   - 已修复：改为使用 `errcode.CodeInvalidPayload`。
2. `orders.auction_id` 从普通索引升级为唯一索引时，若复用旧索引名，AutoMigrate 可能无法真正建立唯一约束。
   - 已修复：为订单竞拍唯一约束使用显式新索引名 `idx_orders_auction_id_unique`，并在 `CreateFromAuction` 中用幂等创建兜底。
3. `AuctionTimer` 中存在旧的单独订单创建方法，容易让后续维护重新绕过统一保证金逻辑。
   - 已修复：删除旧路径，成交副作用统一走 `handleAuctionCompleted`。

**验证结果**

- `go test ./...` 通过。
- `pnpm --filter @jingpai/mobile build` 通过。
- `pnpm --filter @jingpai/merchant build` 通过。
