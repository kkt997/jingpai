# 竞拍大师 — 前端重构与迭代开发总结

本文档详尽记录了针对「实时竞拍大师」前端（包括用户端 H5 与商家端 PC）所进行的主题重构、核心交互迭代及架构优化。

---

## 1. 前端架构与构建重构

### 1.1 源码直编译方案 (TS 编译路径修复)
* **问题背景**：原本 `@jingpai/shared` 作为 monorepo 的子包，在编译时缺少构建产物，而 `@jingpai/mobile` 和 `@jingpai/merchant` 的 `tsconfig.json` 配置了 references 工程引用。导致容器内编译时由于缺少 `dist/index.d.ts` 声明报错 `TS6305`，阻断打包。
* **重塑逻辑**：
  1. 移除了移动端和商家端 `tsconfig.json` 中的 `"references"` 路径块。
  2. 将 `packages/shared/package.json` 的主入口 `"main"` 和 `"types"` 直接指向 `"src/index.ts"` 源码。
  3. 使得 Vite 构建时直读源码进行打包，热重载 (HMR) 速度极大加快，容器构建时间大幅降至 **5s** 左右。

### 1.2 Docker 镜像多阶段构建
* 前端 `Dockerfile` 中摒弃了冗余的 `RUN pnpm --filter @jingpai/shared build` 指令，改为整体直读打包。
* 利用 `Nginx` 完成移动端 H5 路径指向 `/` 和商家端 PC 路径指向 `/merchant/` 的反向代理路由规则，并暴露于宿主机 **`8888`** 端口下。

---

## 2. 全局暗黑毛玻璃风格 (Shadcn/ui & Tailwind)

### 2.1 主题系统配置
* 为移动端 H5 注入了 **暗色调（Zinc-950）** 主题系统，采用微渐变霓虹环境光和极细分割线（`border-zinc-800/80`），营造高净值奢华感。
* 为商家端 PC 注入了 **干净亮白调（Zinc-50/100）** 极简设计，使得统计图标与卡片清晰规范。
* 彻底移除了原先粗糙的 Emoji 表情，全面引入 `lucide-react` 精致矢量图标库，提升视觉高级感。

### 2.2 情绪反馈动效
* **临结发光警报**：重塑了 `CountdownTimer.tsx` 倒计时药丸组件，在竞拍临近结束的最后 **10 秒**，倒计时背景和文字会自动流转为动感红橙发光的呼吸灯样式。
* **通知悬浮反馈**：全局的领先、被超越、中标和流拍通知引入 `framer-motion` 自顶向下缓动拉出，停留 2-4 秒后自动渐隐淡出。

---

## 3. 互动聊天弹幕与领先者跑马灯

### 3.1 下半屏页签重构
* 改变了旧版出价排行榜占据了过多屏幕垂直空间的痛点，将下半屏重构为 Tab 切换面板：
  * `💬 互动聊天`（默认开启）：展示用户发言和系统加价动态，并搭载极简常驻聊天输入框。
  - `🏆 竞拍排行`：展示出价排行榜（设置 `max-h-56` 及 `overflow-y-auto` 支持局部独立滚动）。
* 页签之上挂载了常驻单行**领先者跑马灯 (`Flame`) 横幅**：明拍状态下显示出价人别名和领先价，盲拍下脱敏金额只提示当前已有 N 人出价，让用户在聊天时也不会错过价格变动。

### 3.2 弹幕流自动滚动与消息区隔 (`ChatList.tsx`)
* 新建 [ChatList.tsx](file:///d:/jingpai/frontend/packages/mobile/src/components/ChatList.tsx) 组件，利用 `useRef` 及 `useEffect` 侦听消息数组，在新消息到达时自动执行 `scrollIntoView` 平滑滑至最底端。
* **消息视觉分类**：
  * **系统公告**：淡黄微金色卡片搭配 `Bell` 图标，展现如起拍、出价、延时和结拍信息。
  * **本人弹幕**：红橙至珊瑚渐变气泡带“我”字 Badge，右侧对齐。
  * **观众弹幕**：墨黑半透明卡片，左侧对齐。
* **优雅降级模拟**：在 `chatStore.ts` 中拦截发送，往本地强行插值一条 `isMe: true` 的弹幕，以此保证后端聊天广播未就绪时界面依然完全可玩。

---

## 4. 拍品展品柜 (Showcase) 橱窗设计

### 4.1 拍品柜抽屉组件 (`ShowcaseDrawer.tsx`)
* 主界面标题下方新增挂载了金色微亮 `✨ 本场拍品柜 >` 按键，点击可由底部拉起拍品展橱抽屉。
* **时序拍品分类展示**：
  * **已结拍**：图片加上 Grayscale 灰度及亮度滤镜，高亮“已成交”标识与得主别名。
  * **竞拍中**：包裹在红橙呼吸卡片内，显示“当前价”并提供“立即出价”按钮，点击可一键关闭抽屉并切到排行榜 Tab 引导出价。
  * **待开拍**：亮灰色调，显示“起拍价”与“详情”。

### 4.2 双向抽屉细节联动
* 将原有的商品详情抽屉升级重构为了通用的 `selectedProductId` 驱动抽屉，从而能为拍品柜中点击的任何非活跃商品提供细节渲染。
* 当用户在拍品柜点击任何一件历史或未来商品时，均会自动滑出 [ProductDetailDrawer.tsx](file:///d:/jingpai/frontend/packages/mobile/src/components/ProductDetailDrawer.tsx) 展示多图左右轮播、宝贝描述与参数。

### 4.3 状态机与 WS 实时更新
* 状态机 `showcaseStore.ts` 在初始化时会自动请求接口并优雅降级为本地 mock 拍品数据链（包含已拍、在拍、未拍三阶商品）。
* 在 `dispatcher.ts` 收到 `MSG_NEW_BID` (新出价)、`MSG_AUCTION_START` (开拍) 和 `MSG_AUCTION_END` (结拍) 等事件时，秒级同步修改拍品柜中对应拍品的价格、状态与最终中标人，实现完美的直播同步演示。
